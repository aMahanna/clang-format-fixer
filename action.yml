name: 'clang-format-fixer'
description: 'Google-style auto-formatter, wip.'
author: 'aMahanna'
inputs: 
  myInput:
    description: 'Input to use'
    default: 'world'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.myInput }}
